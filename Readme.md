This code tokenizes words and gives the result of how many times that word is in that 
file ommiting the any special characters. Using the Regular expression library
and use the constructor re.sub().
